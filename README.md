This is a basic project done as part of Slice Technologies' technical examination.

It contains JUnit unit tests and RSpec functional tests.

Store text files for execution in `src/main/resources/txtfiles`.